# --- CampusFix AI (optional upgrades; everything works WITHOUT these) ---
# Real multimodal vision + LLM reasoning. Leave empty for offline heuristic mode.
GEMINI_API_KEY=
GOOGLE_API_KEY=
GEMINI_MODEL=gemini-3.6-flash

# Optional: better duplicate embeddings (zero-download fallback otherwise)
# ST_MODEL=all-MiniLM-L6-v2
# Optional: CLIP zero-shot vision
# CLIP_MODEL=openai/clip-vit-base-patch32

# Optional: MongoDB (app runs in-memory without it)
# MONGO_URI=mongodb://localhost:27017
# MONGO_DB=campusfix
